{
  "name": "360step",
  "version": "1.0.0",
  "private": true,
  "description": "360step urban navigator (static + lightweight server config).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.2"
  }
}

